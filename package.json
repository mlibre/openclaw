{
	"name": "openclaw-render",
	"version": "1.0.0",
	"private": true,
	"dependencies": {
		"openclaw": "latest"
	},
	"scripts": {
		"start": "OPENCLAW_CONFIG_PATH=$(pwd)/openclaw.json openclaw gateway --bind lan --port $PORT"
	}
}